# Карта событий города

Веб-приложение для просмотра и управления событиями города с интерактивной картой, фильтрацией по интересам, системой отзывов и возможностью сохранения событий.

## Функциональность

### Основные возможности:
- 🗺️ **Интерактивная карта** - просмотр событий на карте с маркерами
- 📅 **Список событий** - детальная информация о каждом событии
- 🔍 **Фильтрация** - по категориям и интересам
- ⭐ **Система отзывов** - оценка и комментарии к событиям
- 💾 **Сохранение событий** - возможность сохранить понравившиеся события
- 📱 **Адаптивный дизайн** - работает на всех устройствах

### Структура данных:
- **События** - название, описание, дата/время, место, координаты, категория, интересы, цена
- **Пользователи** - имя, email, интересы
- **Отзывы** - оценка (1-5), комментарий, автор, дата
- **Сохраненные события** - связь пользователя с событием

## Установка и запуск

### Требования:
- Python 3.7+
- pip

### Установка:

1. Клонируйте репозиторий или скачайте файлы проекта

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Запустите приложение:
```bash
python app.py
```

4. Откройте браузер и перейдите по адресу: `http://localhost:5000`

## Использование

### Просмотр событий:
- На главной странице отображается карта города с маркерами событий
- Список событий показывает детальную информацию
- Клик по маркеру на карте показывает краткую информацию о событии

### Фильтрация:
- Используйте выпадающие списки для фильтрации по категориям и интересам
- Нажмите "Применить фильтры" для обновления результатов

### Отзывы:
- Перейдите на страницу события для просмотра отзывов
- Добавьте свой отзыв, указав имя, оценку и комментарий

### Сохранение событий:
- Нажмите кнопку "Сохранить" на любом событии
- Введите ваше имя для создания профиля
- Просматривайте сохраненные события по ссылке

## Структура проекта

```
├── app.py                 # Основной файл приложения
├── requirements.txt       # Зависимости Python
├── README.md             # Документация
├── templates/            # HTML шаблоны
│   ├── base.html         # Базовый шаблон
│   ├── index.html        # Главная страница
│   ├── login.html        # Страница входа
│   └── register.html     # Страница регистрации
└── events.db             # База данных SQLite (создается автоматически)
```

## Технологии

- **Backend**: Flask, SQLAlchemy
- **Frontend**: HTML5, CSS3, JavaScript, Bootstrap 5
- **Карты**: Leaflet.js, OpenStreetMap
- **База данных**: SQLite
- **Иконки**: Font Awesome

## Тестовые данные

При первом запуске приложение автоматически создает тестовые события:
- Концерт классической музыки
- Мастер-класс по программированию
- Фестиваль уличной еды
- Йога в парке
- Выставка современного искусства

## API Endpoints

- `GET /` - Главная страница
- `GET /api/events` - API для получения событий с фильтрацией
- `GET /event/<id>` - Страница конкретного события
- `POST /add_review` - Добавление отзыва
- `POST /save_event` - Сохранение события
- `GET /saved_events/<username>` - Сохраненные события пользователя

## Разработка

Для разработки рекомендуется:
1. Использовать виртуальное окружение Python
2. Установить Flask в режиме разработки: `pip install -e .`
3. Включить режим отладки в app.py: `app.run(debug=True)`

## Лицензия

Этот проект создан в образовательных целях для курсовой работы.

## Решение проблем

### Проблема с кириллическими символами в пути
Если у вас возникают проблемы с сохранением файлов из-за кириллических символов в пути проекта, используйте папку с латинскими символами:
- Вместо `D:\универ\4 курс\курсач` используйте `D:\univer\4_kurs\kursach`

### Проблемы с запуском
1. Убедитесь, что Python установлен и добавлен в PATH
2. Установите зависимости: `pip install -r requirements.txt`
3. Запустите из папки проекта: `python app.py`
